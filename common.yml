---
# This playbook install common dependencies
- name: installing common apt dependencies, pip python packages as well as java and lmod
  hosts: think_twice
  remote_user: "{{ sudo_guy }}"
  roles:
            # apt-get installation
            - common
            # lmod installed in /opt directory
            - lmod

  become: true
