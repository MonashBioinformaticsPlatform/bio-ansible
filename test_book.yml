---
# This playbook deploys bioinformatics server

- name: apply common configurations to all nodes
  # This block should really be `all`. I replaced it with 'bioifnformatics' for testing purposes
  # This block should really be `all`. I replaced it with 'bioifnformatics' for testing purposes
  #hosts: all
  hosts: test
  remote_user: ubuntu
  roles:
          - server_update
          - make_sw_guy
          - common
  sudo: yes

- name: Set up bioinformatiky tools
  hosts: test
  remote_user: sw-installer
  roles:
          - bio_tools
          - check
  sudo: yes
