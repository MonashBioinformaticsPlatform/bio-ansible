---
#This playbook deploys RNAsik-pipe with all relevant tools

- name: Installing dependencies, Java and lmod
  hosts: home
  remote_user: "{{ main_guy }}"
  roles:
            # apt-get installation
            - common
            # lmod installed in /opt directory
            - lmod
            # java installed throught ppa
            - java

  become: true

- name: Installing bioinformatics tools
  hosts: home
  remote_user: "{{ main_guy }}"
  roles:
          # make container for all other installation
          - make_dirs
            #- bds
          # install everyting into one container
          - bio_tools
