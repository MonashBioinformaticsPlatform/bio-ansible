- name: install nginx
  apt: name=nginx state=latest update-cache=yes force=yes
  sudo: yes
  # Do I need `when`..?
  #when: license_check.stdout.find('true') == -1 
  
  #- name: Make a copy of the default file
  #  copy: src=/etc/nginx/sites-available/default dest=/etc/nginx/sites-available/default.bak
  #        owner=root group=root mode=0644

- name: Set the default config file
  template: src=templates/nginx-default-config dest=/etc/nginx/sites-available/
#- replace: dest=/etc/nginx/sites-available/default regex='location\s\/\s{[]
