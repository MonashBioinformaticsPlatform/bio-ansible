---

- name: pip install python packages
  pip:
    name: "{{ item }}"
    state: latest
  with_items:
    - virtualenv
    - nesoni
    # bioinformaticky packages
    - pysam
    - gffutils
      #- htseq
  tags: pip
