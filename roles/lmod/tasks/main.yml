---

- include_tasks: lmod.yml
  tags: lmod
  vars:
    version: "{{ lmod_version }}"

# This adds a standard set of modules the load by default, and some task-specific sets
# - include: lmod_stdenv.yml
