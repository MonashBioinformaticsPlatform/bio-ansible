---
# This will install and set up nginx 

- name: install nginx
  apt: name=nginx state=latest update-cache=yes force=yes
  sudo: yes
  
- name: Set the default config file
  template: src=nginx-default-config dest=/etc/nginx/sites-available/
