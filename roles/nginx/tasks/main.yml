---
# This will install and set up nginx 

- name: Install nginx
  apt: name=nginx state=latest update-cache=yes force=yes
  sudo: yes
  
- name: Copy nginx configuration file
  template: src=nginx-default-config dest=/etc/nginx/sites-available/default.conf
  notify: restar nginx
