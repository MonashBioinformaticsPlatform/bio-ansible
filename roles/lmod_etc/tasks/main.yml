---

- include_tasks: lmod_etc.yml
  tags: ["lmod_etc", "lmod"]
  vars:
    version: "{{ lmod_version }}"

# This is if you want a set of modules loaded by default
# - include_tasks: lmod_stdenv.yml
#   tags: ["lmod_etc", "lmod"]
