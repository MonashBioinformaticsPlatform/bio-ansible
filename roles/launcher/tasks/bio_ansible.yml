---

- name: download bio-ansible {{ version }}
  git:
    repo: https://github.com/serine/bio-ansible
    dest: "{{ tmp_dir }}/bio-ansible-{{ version }}"
    version: "{{ version }}"

  become_user: "{{ main_guy }}"
  tags: bio_ansible
