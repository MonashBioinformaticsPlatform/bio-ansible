---

- name: copy krb5 config /etc
  tags: user_auth
  template:
    src: krb5.conf
    dest: /etc/krb5.conf
    owner: root
    mode: 0644

- name: create ldap-user group
  tags: user_auth
  group:
    name: ldap-user
    gid: 1190
